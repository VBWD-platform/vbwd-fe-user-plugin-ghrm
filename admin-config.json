{
  "tabs": [
    {
      "id": "catalogdisplay",
      "label": "Catalogue Display",
      "fields": [
        {
          "key": "catalogue_route_prefix",
          "label": "Catalogue Route Prefix (read-only)",
          "component": "input",
          "inputType": "text",
          "description": "Fixed at /category — routes are registered at app boot and cannot be changed at runtime"
        },
        {
          "key": "packages_per_page",
          "label": "Packages Per Page",
          "component": "input",
          "inputType": "number",
          "min": 1,
          "max": 100,
          "description": "Number of packages shown per catalogue page"
        },
        {
          "key": "related_packages_limit",
          "label": "Related Packages Limit",
          "component": "input",
          "inputType": "number",
          "min": 0,
          "max": 20,
          "description": "Maximum number of related packages shown on a detail page"
        },
        {
          "key": "show_in_nav",
          "label": "Show in Navigation",
          "component": "checkbox",
          "description": "Show Software link in the main user navigation menu"
        }
      ]
    }
  ]
}
